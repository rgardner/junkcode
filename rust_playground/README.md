# Rust Playground

## Getting Started

### Building and Running

```sh
# Build
invoke build [--release]
# Run binaries
invoke run calculator
invoke run json
invoke run thread_sum
```

### Formatting

```sh
invoke format
```

### Linting

```sh
invoke lint
```
